프리미엄 품질 옵션 소형 패치

적용 내용
- 프리미엄 images.edit 호출에 quality 추가
- 기본값: PREMIUM_IMAGE_QUALITY = "xhigh"

사용법
1. 현재 적용 중인 app.py를 이 폴더에 넣기
2. APPLY_PATCH.cmd 실행
3. 생성된 app_patched.py를 app.py로 이름 변경
4. 업로드 후 앱 재시작

비고
- 필요하면 나중에 환경변수 PREMIUM_IMAGE_QUALITY 로 high / xhigh / max 조정 가능
