# Stable premium renderer patch

## 변경 방향
최근 1080x1920 + 자유 재렌더링 방식에서 섹션 잘림, 정렬 오차, 푸터/표 겹침, 적용분야 카드 깨짐이 발생하여
프리미엄 생성 방식을 안정형 규칙 기반 렌더러로 전환했습니다.

## 핵심 변경
1. `gpt-image-2`로 프리미엄 페이지 전체를 다시 그리는 흐름을 사용하지 않음
2. `make_high_quality_infographic_image(...)`를 프리미엄 출력의 기본 생성기로 사용
3. 적용분야/제품, 시장현황, 기술개요, 대표도면, 기술 경쟁력, 지식재산권, 문의처를 코드 좌표계로 직접 배치
4. 프리미엄 PDF는 다시 A4 300dpi급 PDF로 저장
5. 버튼/설명 문구를 안정형 프리미엄 렌더러 기준으로 수정

## 기대 효과
- 섹션 잘림 감소
- 하단 IP/문의처 겹침 감소
- 로고/QR/대표도면 보존성 향상
- 시장현황 그래프와 적용분야 카드의 레이아웃 안정성 향상

## 수정 파일
- app.py
